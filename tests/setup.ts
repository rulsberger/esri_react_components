// Import jest-dom for extended DOM assertions
import '@testing-library/jest-dom';

// You can add any global setup code here
// For example, you can mock global objects or configure global settings

// Example: Mocking a global object
// globalThis.someGlobalObject = jest.fn();